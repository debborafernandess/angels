<h1><%= @market.name %></h1>
<%= render partial: 'projects/project', collection: @market.projects %>
<%= link_to 'Criar Grupo', new_group_path %>
<% @market.groups.each do |group| %>
  <%= link_to group.name, group %>
<% end %>
