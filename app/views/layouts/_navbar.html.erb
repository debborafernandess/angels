<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">Angels</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-collapse">
      <div class="navbar-right">
        <% if investor_signed_in? %>
        <div class="investor-nav">
          <span>Olá <%= current_investor.name %></span>
          <%= link_to 'Meus Grupos', groups_path, class: "btn btn-default navbar-btn" %>
          <%=  link_to 'Sair', destroy_investor_session_path, method: :delete, class: "btn btn-default navbar-btn" %>
        </div>
          <% elsif entrepreneur_signed_in? %>
        <div class="entrepreneur-nav">
            <span>Olá <%= current_entrepreneur.name %></span>
            <%=  link_to 'Sair', destroy_entrepreneur_session_path, method: :delete, class: "btn btn-default navbar-btn" %>
        </div>
        <% else %>
          <%=  link_to 'Sou Investidor', new_investor_registration_path, class: "btn btn-default navbar-btn" %>
          <%=  link_to 'Sou Empreendedor', new_entrepreneur_registration_path, class: "btn btn-default navbar-btn" %>
        <% end %>
      </div>
    </div>
  </div>
  <!-- /.container-fluid -->
</nav>
